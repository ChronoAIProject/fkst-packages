# Existing lossy content-truncation inventory for the source_ref/content_fetch migration.
# Format: path|function|cap|sink|issue=#N|why=<one-line root-cause owner reason>
# New entries fail CI. Remove entries only when full content is rehydrated from
# source_ref/content_fetch instead of being capped into reliable payloads or prompts.
