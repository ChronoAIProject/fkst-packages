local core = require("core")

return {
  adapter_error_message = core.adapter_error_message,
  comment_request = core.comment_request,
  failed_result = core.failed_result,
  validate_navigation = core.validate_navigation,
  validate_request = core.validate_request,
}
