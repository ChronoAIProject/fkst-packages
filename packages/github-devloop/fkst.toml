kind = "package.composed"
name = "github-devloop"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "testkit", "forge", "devloop"]

[event_deps]
packages = ["github-proxy", "consensus", "github-devloop-decompose"]
