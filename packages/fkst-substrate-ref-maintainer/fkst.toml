kind = "package.composed"
name = "fkst-substrate-ref-maintainer"
persistence_class = "saga"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "forge", "devloop"]

[event_deps]
packages = ["github-proxy"]
